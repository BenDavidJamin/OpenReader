define(["jquery", "underscore", "backbone"], function($, _, Backbone){

  /**
   * [clip contains the id of the starting point of clip.
   * The text contents. 
   * The document id in the database?]
   * Three parameters perhaps.
   * I'm not sure if anything else is needed. 
   * @type {[type]}
   */
  var clip = Backbone.Model.extend({

  });

  return clip;

});